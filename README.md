# College_mini_projects
I have worked together with my teammates to complete my mini-projects in my college . 

#1) Multiple Form Filler:
                         Multiple Form Filler is used to fill the multiple forms using selenium to automate the chrome or firefox browser according to the browser it uses the drivers to fill the data from the sql to fetch the user datas to fill the particular field of the datas in the form. 

how it works💻:
       once the students datas are stored at the sql database then connect the sql to fetch the users data.

user fill the databases details -> then it show the success message connected ->select browser-> Enter URL to Fill-> select Waiting time for each fills (ex: 2sec for each fill)-> press button -> shows the automate Filling form.
